# 视频审核工作流

每个片子一个文件夹，视频和审核结果放一起。

---

## 1. 放视频

在仓库根目录下：

```text
视频审核/
└── <片名>/                 # 每个片子单独一个文件夹
    ├── your_video.mp4      # 待审核视频
    └── 场景.txt            # 可选：场景描述（推荐）
```

示例：

```bash
mkdir -p 视频审核/测试视频1
# 把 your_video.mp4 拷进去
```

**规则：**
- 不要把多个无关片子丢进同一个文件夹
- 片名文件夹名随意，能区分即可（如 `雨夜撑伞`、`测试视频1`）
- 视频格式一般为 `.mp4`

---

## 2. 写场景描述

审核需要对照基准。建议写成 `场景.txt`，写清：

- **角色**：外观、服装
- **环境**：地点、时间、天气
- **动作与镜头**：在做什么、镜头怎么动
- **必须出现的元素**：硬性要求

---

## 3. 跑审核

在 `showvi/` 目录下执行：

```bash
cd showvi
source .venv/bin/activate

python run_critique_and_ref.py \
    --video  ../视频审核/测试视频1/your_video.mp4 \
    --scene  "$(cat ../视频审核/测试视频1/场景.txt)" \
    --output ../视频审核/测试视频1
```

路径相对 `showvi/`；也可用绝对路径。

---

## 4. 看结果

跑完后目录变成：

```text
视频审核/测试视频1/
├── your_video.mp4
├── 场景.txt
└── 审核_YYYYMMDD_HHMMSS/    # 每次运行自动创建
    ├── 审核结果.json
    ├── 原帧/
    ├── 参考图/
    └── 审核报告.md
```

打开 `审核报告.md` 即可查看结论。

---

## 常用变体

| 需求 | 做法 |
|------|------|
| 只审核、不出参考图 | 加上 `--skip-ref --skip-report` |
| 续跑已有审核 | 加 `--session ../视频审核/测试视频1/审核_…` 和 `--skip-critique` |
| 更严 / 更松 | `--strictness 1`–`4`（默认 `3`） |

完整参数与环境配置见 [README.md](README.md)。
